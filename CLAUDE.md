# Claude Code guidance for this repo

The agent constitution for this repository lives in AGENTS.md (shared by all
agent tools). Claude Code only auto-loads CLAUDE.md, so import it here rather
than duplicating it — AGENTS.md remains the single source of truth.

@AGENTS.md
